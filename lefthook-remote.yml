---
pre-commit:
  commands:
    xmllint:
      glob: "*.xml"
      run: timeout ${LEFTHOOK_XMLLINT_TIMEOUT:-30} lefthook-xmllint {staged_files}

pre-push:
  commands:
    xmllint:
      glob: "*.xml"
      run: timeout ${LEFTHOOK_XMLLINT_TIMEOUT:-30} lefthook-xmllint {push_files}
